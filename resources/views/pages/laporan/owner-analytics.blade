@extends('app')

@section('content')
<div class="container-fluid">
    <div class="row mb-3">
        <div class="col-md-8">
            <h2 class="mb-4">Analytics Pendapatan</h2>
        </div>
        <div class="col-md-4 text-end">
            <form method="GET" action="{{ route('laporan.index') }}" class="row g-2">
                <!-- Period Filter -->
                <div class="col-auto">
                    <select name="period" class="form-select form-select-sm">
                        <option value="daily" {{ $period === 'daily' ? 'selected' : '' }}>Harian</option>
                        <option value="weekly" {{ $period === 'weekly' ? 'selected' : '' }}>Mingguan</option>
                        <option value="monthly" {{ $period === 'monthly' ? 'selected' : '' }}>Bulanan</option>
                    </select>
                </div>
                
                <!-- Date Range -->
                <div class="col-auto">
                    <input type="date" name="start_date" class="form-control form-control-sm" value="{{ $startDate->format('Y-m-d') }}">
                </div>
                <div class="col-auto">
                    <input type="date" name="end_date" class="form-control form-control-sm" value="{{ $endDate->format('Y-m-d') }}">
                </div>
                
                <!-- Tipe Kendaraan Filter -->
                <div class="col-auto">
                    <select name="id_tipe" class="form-select form-select-sm">
                        <option value="all">Semua Tipe</option>
                        @foreach($tipeKendaraan as $tipe)
                            <option value="{{ $tipe->id_tipe }}" {{ $idTipe == $tipe->id_tipe ? 'selected' : '' }}>
                                {{ $tipe->tipe_kendaraan }}
                            </option>
                        @endforeach
                    </select>
                </div>
                
                <div class="col-auto">
                    <button type="submit" class="btn btn-primary btn-sm">Filter</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Summary Cards -->
    @if(is_object($data) && $data->count() > 0)
        @php $firstItem = $data->first(); @endphp
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="card text-center">
                    <div class="card-body">
                        <h6 class="card-title">Total Transaksi</h6>
                        <h3>{{ $firstItem->total_transaksi ?? 0 }}</h3>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-center">
                    <div class="card-body">
                        <h6 class="card-title">Kendaraan Masuk</h6>
                        <h3>{{ $firstItem->total_kendaraan_masuk ?? 0 }}</h3>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-center">
                    <div class="card-body">
                        <h6 class="card-title">Kendaraan Keluar</h6>
                        <h3>{{ $firstItem->total_kendaraan_keluar ?? 0 }}</h3>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-center bg-success text-white">
                    <div class="card-body">
                        <h6 class="card-title">Total Pendapatan</h6>
                        <h3>Rp {{ number_format($firstItem->total_pendapatan ?? 0, 0, ',', '.') }}</h3>
                    </div>
                </div>
            </div>
        </div>
    @endif

    <!-- Data Table -->
    <div class="card">
        <div class="card-header">
            <h5 class="mb-0">Data {{ ucfirst($period) }}</h5>
        </div>
        <div class="card-body table-responsive">
            <table class="table table-sm table-hover">
                <thead class="table-light">
                    <tr>
                        <th>Periode</th>
                        <th class="text-end">Transaksi</th>
                        <th class="text-end">Masuk</th>
                        <th class="text-end">Keluar</th>
                        <th class="text-end">Pendapatan</th>
                    </tr>
                </thead>
                <tbody>
                    @forelse($data as $item)
                        <tr>
                            <td>
                                @if($period === 'daily')
                                    {{ $item->tanggal->format('d/m/Y') }}
                                @elseif($period === 'weekly')
                                    {{ $item['week'] }} ({{ $item['start_date']->format('d/m') }} - {{ $item['end_date']->format('d/m') }})
                                @else
                                    {{ $item['month_name'] }}
                                @endif
                            </td>
                            <td class="text-end">{{ $item->total_transaksi ?? $item['total_transaksi'] }}</td>
                            <td class="text-end">{{ $item->total_kendaraan_masuk ?? $item['total_kendaraan_masuk'] }}</td>
                            <td class="text-end">{{ $item->total_kendaraan_keluar ?? $item['total_kendaraan_keluar'] }}</td>
                            <td class="text-end fw-bold">Rp {{ number_format($item->total_pendapatan ?? $item['total_pendapatan'], 0, ',', '.') }}</td>
                        </tr>
                    @empty
                        <tr>
                            <td colspan="5" class="text-center text-muted">Tidak ada data</td>
                        </tr>
                    @endforelse
                </tbody>
            </table>
        </div>
    </div>

    <!-- Pagination jika perlu -->
    @if(is_object($data) && method_exists($data, 'links'))
        <div class="mt-3">
            {{ $data->links() }}
        </div>
    @endif

    <hr class="my-5">

    <!-- Breakdown per Tipe Kendaraan -->
    <div class="row mb-4">
        <div class="col-md-12">
            <h5 class="mb-3">Breakdown Per Tipe Kendaraan</h5>
        </div>
        @forelse($breakdownTipe as $tipe => $item)
        <div class="col-md-4 mb-3">
            <div class="card">
                <div class="card-body">
                    <h6 class="card-title">{{ $item['tipe'] }}</h6>
                    <table class="table table-sm mb-0">
                        <tr>
                            <td class="text-muted">Jumlah:</td>
                            <td class="fw-bold">{{ $item['jumlah'] }} unit</td>
                        </tr>
                        <tr>
                            <td class="text-muted">Total Revenue:</td>
                            <td class="fw-bold text-success">Rp {{ number_format($item['total_revenue'], 0, ',', '.') }}</td>
                        </tr>
                        <tr>
                            <td class="text-muted">Rata-rata:</td>
                            <td class="text-info">Rp {{ number_format($item['rata_rata'], 0, ',', '.') }}</td>
                        </tr>
                    </table>
                </div>
            </div>
        </div>
        @empty
        <div class="col-md-12">
            <div class="alert alert-info">Tidak ada data breakdown per tipe kendaraan</div>
        </div>
        @endforelse
    </div>

    <!-- Breakdown per Metode Pembayaran -->
    <div class="row mb-4">
        <div class="col-md-12">
            <h5 class="mb-3">Breakdown Per Metode Pembayaran</h5>
        </div>
        @forelse($breakdownMetode as $metode => $item)
        <div class="col-md-4 mb-3">
            <div class="card">
                <div class="card-body">
                    <h6 class="card-title">{{ $item['metode'] }}</h6>
                    <table class="table table-sm mb-0">
                        <tr>
                            <td class="text-muted">Transaksi:</td>
                            <td class="fw-bold">{{ $item['jumlah'] }}</td>
                        </tr>
                        <tr>
                            <td class="text-muted">Total:</td>
                            <td class="fw-bold text-success">Rp {{ number_format($item['total_revenue'], 0, ',', '.') }}</td>
                        </tr>
                        <tr>
                            <td class="text-muted">Rata-rata:</td>
                            <td class="text-info">Rp {{ number_format($item['rata_rata'], 0, ',', '.') }}</td>
                        </tr>
                    </table>
                </div>
            </div>
        </div>
        @empty
        <div class="col-md-12">
            <div class="alert alert-info">Tidak ada data breakdown per metode pembayaran</div>
        </div>
        @endforelse
    </div>

    <!-- Occupancy Rate per Area -->
    <div class="card mt-5">
        <div class="card-header">
            <h5 class="mb-0">Status Slot Parkir Per Area</h5>
        </div>
        <div class="card-body">
            <div class="row">
                @forelse($slotCapasitas as $area => $item)
                <div class="col-md-6 mb-3">
                    <div class="card bg-light">
                        <div class="card-body">
                            <h6 class="card-title">{{ $item['area'] }}</h6>
                            <div class="mb-3">
                                <div class="d-flex justify-content-between mb-1">
                                    <small>Occupancy Rate:</small>
                                    <small class="fw-bold">{{ $item['occupancy_rate'] }}%</small>
                                </div>
                                <div class="progress" style="height: 20px;">
                                    <div class="progress-bar bg-danger" role="progressbar" 
                                         style="width: {{ $item['occupancy_rate'] }}%">
                                        {{ $item['occupancy_rate'] }}%
                                    </div>
                                </div>
                            </div>
                            <table class="table table-sm mb-0">
                                <tr>
                                    <td class="text-muted">Kapasitas Total:</td>
                                    <td class="fw-bold">{{ $item['total_kapasitas'] }} slot</td>
                                </tr>
                                <tr>
                                    <td class="text-muted">Tersedia:</td>
                                    <td class="text-success fw-bold">{{ $item['tersedia'] }} slot</td>
                                </tr>
                                <tr>
                                    <td class="text-muted">Terpakai:</td>
                                    <td class="text-danger fw-bold">{{ $item['terpakai'] }} slot</td>
                                </tr>
                            </table>
                        </div>
                    </div>
                </div>
                @empty
                <div class="col-md-12">
                    <div class="alert alert-warning">Belum ada data slot kapasitas</div>
                </div>
                @endforelse
            </div>
        </div>
    </div>
</div>
@endsection